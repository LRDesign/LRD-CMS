%ul.posts
  - @posts.each do |post|
    .post
      %h3= post.title
      %h4= post.updated_at
      %p= post.content
      = link_to(pages_path(post), "Full text...")

%ul.topics
  - @topics.each do |topic|
    %li= link_to(topic_path(topic), topic.name)

-#.archives
