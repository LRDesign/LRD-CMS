- set_headline "Editing #{@human_name}: '#{@page.title}'"

= link_to 'Show', admin_page_path(@page)
|
= link_to 'Back to List', :action => :index

= render :partial => "form", :locals => {:url => url_for(:action => :update, :id => @page.id)}
